import styled from "@emotion/styled";
import StyledFlexContainer from "./FlexContainer";

const StyledFillAndCenterContainer = styled(StyledFlexContainer)({
  alignItems: "center",
  justifyContent: "center"
});

export default StyledFillAndCenterContainer;
