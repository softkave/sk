import { getReducer } from "../utils";
import PermissionGroupsActions from "./actions";

const permissionGroupssReducer = getReducer(PermissionGroupsActions);

export default permissionGroupssReducer;
